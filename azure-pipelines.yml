trigger:
  branches:
    include:
      - develop

resources:
  repositories:
    - repository: devops
      type: git
      name: Devops

extends:
  template: templates/build-microservice.yml@devops
  parameters:
    appName: "restabarlabello-front"
    imageRepository: "restabarlabello-front"
    secretFileName: "restabarlabello-front-secret.yaml"
    deploymentType: "frontend"
    backendAppName: "restabarlabello-backend"
    subdomain: "restabarlabello"
